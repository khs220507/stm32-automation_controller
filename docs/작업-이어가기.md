# 작업 이어가기

대화를 재개할 때 가장 먼저 확인하는 진행 메모다. 사용자 결정, 완료 보고와 다음 행동을 남긴다. 공식 요구사항·시험 판정은 관리 문서를 따르며, 이 메모가 대신하지 않는다.

## 현재 위치

- 최신 연결 설명(2026-09-15): received 저장 이후 메인 루프의 app_state_run()→protocol_poll_command()→protocol_poll_source()→uart1_try_read_byte() 호출을 설명했다. 한 바이트씩 꺼내며 마지막 바이트에서 위치·길이를 0으로 되돌려 버퍼를 재사용한다. 이는 배열을 지우는 동작이 아니다. 이후 CRLF 확인→PING 명령 변환→app_state.c 실행→RS-485 PONG 응답 흐름을 연결했다. 다음 상세 시작점은 uart1_try_read_byte()의 포인터·위치 처리이며, IRQ 조건문 상세는 남아 있다.
- 최신 분석 범위(2026-09-15): USART1_IRQHandler()의 핵심을 PING 수신 예로 설명했다. incoming은 수집 중인 줄, received는 메인에 전달할 완성 줄이며, received_length는 전달 길이와 대기 여부를 나타낸다. 줄 끝 전에는 반환하고, 줄 끝에서 오류·폐기 상태가 아니며 이전 줄이 소비된 경우에만 복사한다. 수신과 명령 실행의 분리, 송신 중 수신 제외·부분 입력 만료·오류·길이 초과·미소비 줄 보호를 개괄했다. 레지스터 비트와 각 조건문의 상세 분석은 남아 있다.
- 2026-09-15 문서 정리: 사용자 요청으로 현재 TCP·RS-485·UART 및 SPI·I²C·초음파 연결을 [전체 통신 블록구조](설계/전체-통신-블록구조.docx)에 가로 한 장의 편집 가능한 블록도로 정리했다. Word 저장·재열기·1페이지 구성과 렌더링을 확인했다. 코드 분석 재개 위치와 공식 요구사항·시험 판정은 유지한다.
- 현재 작업: 2026-09-15 RS-485 코드 분석 재개. main.c의 초기화 호출 관계와 uart1.c의 함수별 역할, USART1_IRQHandler()의 바이트 수집→한 줄 전달 역할까지 설명했다. 사용자가 기존 UART 분석 이력을 짚어 초기화부터 반복하는 안내를 정정했다. 다음은 Src/uart1.c의 USART1_IRQHandler() 본문 상세다. 로직애널라이저는 계속 보류한다.
- 완료한 준비: 납땜 완료 및 AT044 TXD↔PB6(CN10-17), RXD↔PB7(CN7-21) 연결 완료(2026-09-14 사용자 보고). 전원·A/B를 포함한 전체 배선의 실물 검증을 뜻하지 않는다.
- 최신 배선 변경 보고: 2026-09-14 사용자가 TX–TX/RX–RX 연결안과 전원 연결 안내 후 “연결다함”이라고 보고했다. 직전 표 기준 AT044 TXD↔PB6(CN10-17), RXD↔PB7(CN7-21), VCC↔3.3V(CN7-16), TTL GND↔GND(CN7-20) 연결 완료로 기록한다. 이전 TX/RX 교환 보고 이후의 재연결이며 실물·입출력 방향·통신 성공은 미검증이다.
- 완료한 구현: USART1 115200 8N1 수신·응답, TCP/RS-485 별도 파서와 응답 경로, WPF 연결 방식/COM 선택·시간 초과·기존 센서 표시. C 호스트 시험과 WPF 122개 시험 성공. 최종 펌웨어 다운로드·검증·리셋 완료.
- 실제 결과: 재연결 후 COM6 PING/PONG 3/3 성공, 상태 IDLE/0, W5500=4, MPU6050 식별값=114, 가속도 설정 2G 및 원시값 (-6205, -548, 15809) 수신. 초음파 TIMEOUT,0,0은 측정 성공이 아니다. 이전 COM4 무응답·수신 카운터 0과 구분한다.
- 다음 위치: Src/uart1.c의 USART1_IRQHandler() 본문 상세 분석. 기존 UART 기본 설정·송수신 분석을 반복하지 않는다. USART1/USART2 IRQ 핸들러의 구현·시험 기록은 있으나 상세 분석 완료 기록은 확인되지 않는다. 분석 시작 시 실제 파일과 최신 공식 요구사항을 확인하고 필요한 동작과 근거를 직접 설명한다. 추가 실기 시험·과제·퀴즈를 선행 조건으로 두지 않는다. UART 실기 재개 시에는 마지막 조회에서 사라진 COM3를 다시 확인한다.
- 남은 실기 확인: VCC–GND/A–B 저항, TX/RX 방향, USB 변환기 구조·A/B 연결. 아직 점검 통과로 기록하지 않는다. 현재 펌웨어는 PB7 명령 수신 전까지 PB6를 입력으로 유지한다.
- [구현·시험·제조사 근거](설계/RS485-WPF-구현기록.md). 최신 실행 파일은 software/build/UltrasonicMonitor/bin/Debug/net10.0-windows/UltrasonicMonitor.exe다. 현재 화면의 개별 시험 결과와 센서 경로의 연결 상태는 별개다.

## 이전 디버깅 계획 — 코드 분석 이후 필요 시 참고

목적: PC의 PING이 끊기는 구간을 특정하고 실제 RS-485 센서 응답과 WPF 표시까지 확인한다. 수신 카운터 0만으로 물리 신호가 없거나 배선 불량이라고 확정하지 않는다.

1. **연결 상태 확정:** PC USB 변환기 종류·COM 번호, 케이블 양쪽 A/B, TX/RX 교환 후 최종 핀 연결을 확인한다. 로직애널라이저 모델·입력 허용 전압·사용 프로그램도 확인한다.
2. **멀티미터 확인:** 전원 분리 상태에서 배선 도통·단락을 확인하고, 사용자가 전원을 인가한 뒤 AT044 VCC–GND 전압을 측정한다. 기존 연결의 기대값은 약 3.3 V이며 실제값은 미측정이다. 측정 모드와 프로브 단자를 확인한다.
3. **신호 캡처:** 입력 허용 범위를 확인한 로직애널라이저로 TTL 쪽 모듈 출력과 PB7을 관찰한다. 필요하면 PB6도 함께 확인한다. 일반 로직 입력을 A/B에 바로 연결하지 않는다. Codex가 동일한 PING을 전송하고 115200 8N1 수신 내용과 파형을 비교한다. 실제 캡처 설정·접지는 장비 확인 후 정한다.
4. **결과에 따른 원인 분리:** 모듈 TTL 출력부터 신호가 없으면 PC 변환기·A/B 경로·모듈 전원/방향을 확인한다. 모듈 출력에는 있고 PB7에는 없으면 그 사이 배선을 확인한다. PB7에 정상 PING이 있는데 카운터가 0이면 펌웨어 핀 설정·USART·인터럽트 경로를 점검한다. 수신은 되지만 내용이 틀리면 통신 설정·신호 품질을 확인한다.
5. **응답과 UI 검증:** 정상 PING 수신 후 명령 해석→PB6 응답→PC 수신 순서를 확인한다. PONG이 확인되면 센서 명령, 반복 요청, WPF 표시·시간 초과/재연결을 확인한다. 조건·실제값·파형·로그를 함께 기록하며 센서 오류 응답을 측정 성공으로 처리하지 않는다.

완료 조건: 실제 RS-485 경로의 PING/PONG, 센서 응답 및 그 결과의 WPF 표시를 증거로 확인. 물리 계측은 사용자가 수행하고 Codex는 PC 송신·펌웨어 분석·필요한 수정·로그 정리를 맡는다.

## 기록 방법

- 2026-09-15 사용자 요청: 코드 분석도 매번 별도 요청 없이 누적한다. 날짜, 파일·함수, 실제 설명한 범위, 남은 범위와 다음 시작점을 기록한다. Codex 설명, 사용자 분석 완료 보고, 구현·실기 검증을 구분하며 설명만으로 사용자 이해 완료를 판정하지 않는다.
- 사용자가 작업 완료, 설계 선택 또는 다음 행동을 알려주면 별도 기록 요청을 기다리지 않고 이 파일을 갱신한다.
- 위의 현재 위치를 갱신하고, 아래 이력에 날짜·결정 또는 수행 내용·근거·남은 일을 짧게 누적한다.
- 기록일과 실제 작업일을 구분한다. 사용자 보고와 도구·계측 확인을 구분하고 확인하지 않은 완료나 시험 결과를 만들지 않는다.
- 경험 요약에는 핵심 경험만 남긴다. 세부 재개 위치는 이 파일로 연결해 중복 관리를 피한다.

## 진행 이력

### 2026-09-15 — received 이후 명령 처리 연결 설명

- 사용자 질문에 따라 완성 줄 소비 함수 uart1_try_read_byte()의 호출자, 바이트 전달과 마지막 바이트 소비 시 버퍼 해제, PING 해석·응답까지 설명했다. 기존 프로토콜 전체 분석을 반복한 것이 아니라 RS-485 경로 연결을 확인한 범위다. 새로운 구현·실기 시험은 없다.

### 2026-09-15 — USART1_IRQHandler 핵심 역할 설명

- 사용자 질문: USART1_IRQHandler의 핵심. 바이트 수집→줄 끝 확인→완성 줄 전달을 PING 예와 incoming/received/received_length의 역할로 설명했다.
- 오류 방어는 개괄만 설명했으며, 레지스터 비트·인터럽트 진입 과정·조건문별 상세 분석이나 새로운 시험은 수행하지 않았다. 다음은 함수 앞부분부터 상세 분석이다.

### 2026-09-15 — 코드 분석 기록 요청 및 재개 위치 정정

- 사용자 요청: “코드분석한것도 기록에 남겨서”. 이후 분석 범위를 파일·함수별로 누적하고 다음 대화에서 이미 본 범위를 반복하지 않도록 한다.
- 설명한 범위: main()→app_state_run()의 INIT 처리→uart1_init() 호출 관계, uart1.c의 초기화·수신·전달·송신 함수 역할. USART1_IRQHandler()는 바이트를 모아 줄 끝에서 메인 처리에 전달하며 명령 해석·PONG 생성은 후속 처리임을 설명했다.
- 사용자 정정: UART는 전에 분석했다는 지적. 기존 경험 요약의 UART 송수신 구현·분석 이력과 일치한다. IRQ 상세 분석은 완료 여부를 확인할 기록이 없으며 미수행으로 단정하지 않는다.
- 남은 범위: USART1_IRQHandler() 본문 상세, 관련 버퍼와 오류·시간 제한 처리. 다음 시작점은 해당 함수다. uart1_init() 상세 분석 완료나 새로운 구현·시험 결과로 기록하지 않는다.

### 2026-09-14 — Git 업로드 및 다음 날 코드 분석 결정

- 사용자 요청: “깃올리자 내일부터는 코드 분석 가면 될 듯하다”. 오늘 통신 구현·검증 결과와 재개 기록을 커밋·푸시하고, 2026-09-15부터 코드 분석을 이어가는 것으로 기록한다.
- 코드 분석 완료로 판정하지 않으며 물리 파형·초음파 TIMEOUT·RS-485 센서 UI의 남은 실기 확인은 그대로 둔다. 이전 디버깅 계획은 참고용으로 보존한다.


### 2026-09-14 — TCP·RS-485·UART 개별 시험 UI 구현 및 실기 확인

- 사용자 요청: RS-485를 명확하게 표시하고 TCP·RS-485·UART를 모두 분리해 시험하며, UART도 별도 영역으로 둔다.
- 구현: 통신 개별 시험 탭에 세 카드와 각 주소/포트 입력·PING 버튼·상태·시간·로그를 분리했다. 개별 시험은 각각 연결→PING→PONG 확인→연결 해제하며 센서 선택 경로와 별개다. UART/RS-485 동일 포트 동시 사용 및 센서 연결과의 충돌을 차단한다. 센서 화면은 별도 탭, 센서용 연결 설정과 하단 경로 표시는 해당 탭에서만 보인다.
- 검증: 최종 dotnet test 128개 성공. TCP 루프백 실제 소켓에서 분할 응답·에코·시간 초과·연결 종료, 모형 응답에서 카드/로그 격리와 COM 충돌, 두 화면 크기에서 배치를 확인했다. 최초 탭 분리 뒤 기존 배치 시험이 숨겨진 센서 탭을 측정해 실패한 항목은 센서 탭 선택 후 측정하도록 수정했다.
- 실제 UI 시험: 23:19 RS-485(COM6)만 클릭했을 때 나머지 두 카드는 미시험 유지. UART(COM3) 클릭 후 TCP 미시험 유지·RS-485 결과 유지. TCP(169.254.100.2:5000) 클릭 후 각 카드에 별도 PONG과 시간·로그 확인. [실기 로그](측정/2026-09-14-RS485-WPF/통신-개별시험-실기로그.txt), [실기 화면](측정/2026-09-14-RS485-WPF/통신-개별시험-화면.png).
- 최종 센서 설정 숨김·버튼 표시 수정 후 128개 자동 시험 통과. 새 프로그램 실행 후 재확인 중 COM3가 Windows 장치 목록에서 사라져 최종 UI에서 UART 실기를 재실행하지 않았다. 원인을 USB 분리라고 확정하지 않는다. 앞선 실제 성공과 시점을 구분한다. 펌웨어·공식 관리 문서·버전은 변경하지 않았다.


### 2026-09-14 — 로직애널라이저 보류 및 COM6 프로그램 통신 성공

- 사용자 요청: 이전 배선 오류 가능성을 제시하며 로직애널라이저 없이 프로그램으로 먼저 시험하도록 요청했다. 배선 오류 확정 보고로 해석하지 않는다.
- Windows 장치 조회에서 USB-SERIAL CH340(COM6), ST-LINK(COM3)을 확인했다. 기존 RS485-실기시험.ps1을 PortName=COM6, PingCount=3, 115200 8N1, 응답 제한 800 ms로 실행했다. 종료 시 포트를 해제했다.
- 실제 결과: PING 3회 모두 OK,PING,PONG. GET_STATUS=IDLE,0; CHECK_W5500=4; CHECK_MPU6050=114; CONFIG_ACCEL=2G; READ_ACCEL=-6205,-548,15809. CHECK_HCSR04=TIMEOUT,0,0으로 초음파 거리 측정 성공은 아니다.
- 증거: [RS-485 실행 로그](측정/2026-09-14-RS485-WPF/RS485-실기시험-로그.txt)의 23:01:19 기록. 펌웨어 변경·다운로드·로직 캡처·WPF UI 검증은 하지 않았다. 배선과 COM 포트가 이전 실패 때와 달라 단일 원인을 확정하지 않는다. 공식 요구사항 0.2-draft의 부분 승인·검토 중 상태를 직접 확인했으며 공식 판정은 변경하지 않았다.


### 2026-09-14 — TX–TX/RX–RX 및 전원 재연결 완료 보고

- 사용자가 AT044가 RX–RX/TX–TX 연결인 것 같다고 제시한 뒤, VCC/GND를 포함한 연결표 안내에 “연결다함”이라고 보고했다. 위 현재 위치에 직전 표 기준 연결을 반영했다.
- A+↔A+, B−↔B−도 직전 안내에 포함됐으나 단자별 실물 확인은 하지 않았다. 전원 분리 여부·재인가·재측정·PING 성공·로직애널라이저 연결 완료를 뜻하지 않는다. 기존 약 3.2 V 측정 보고와 무응답 시험은 재연결 전 결과다.
- 다음은 기존 계획대로 로직애널라이저 모델·사용 프로그램·입력 허용 전압 확인 후 캡처 준비다. 이번 턴에 송신·다운로드·실기 시험은 수행하지 않았다.

### 2026-09-14 — 로직애널라이저 연결 안내

- 사용자 요청으로 AT044 TTL GND와 TXD/RXD를 두 입력 채널에서 관찰하는 연결 방법을 안내한다. 선 색깔이 아닌 장비의 GND·채널 표기를 기준으로 하고, 기존 MCU↔AT044 배선을 유지한 병렬 측정이다.
- 장비 모델과 3.3 V 입력 지원은 아직 미확인이다. 안내를 실제 연결·캡처 완료로 기록하지 않는다. 추가 PC 송신이나 다운로드는 하지 않았다.
- 일반 연결 근거: [Saleae Setup](https://www.saleae.com/support/getting-started/setup)의 회로 GND 연결 및 측정 노드에 신호 입력 연결 안내. 사용자 장비를 Saleae 제품으로 확정한 것은 아니다.

### 2026-09-14 — TXD와 RXD 모두 약 3.2 V 확인 보고

- 사용자가 “둘다”라고 명확히 했다. 직전 3.2 V 보고를 TXD–TTL GND와 RXD–TTL GND 각각의 사용자 측정값으로 반영한다. DC 전압 모드·통신 요청 없는 상태로 측정하도록 안내했으며 파형 증거는 아직 없다.
- 전원과 두 신호 핀의 정지 상태 전압 보고를 확보했다. 이 값만으로 실제 입출력 방향·배선 연결·데이터 전달을 판정하지 않는다. 다음은 로직애널라이저 모델과 프로그램 확인 후 파형 캡처 준비다.

### 2026-09-14 — 신호 핀 전압 3.2 보고, 대상 확인 대기

- TXD–GND와 RXD–GND 각각의 전압 질문에 사용자가 “3.2”라고 답했다. 문맥상 3.2 V 보고로 기록하되, 어느 핀인지 또는 양쪽 모두인지는 확정하지 않는다. 추가 송수신·다운로드는 하지 않았다.

### 2026-09-14 — AT044 전원 약 3.2 V 사용자 보고

- 측정 안내 조건: 전원 인가 상태, 멀티미터 DC 전압 모드, AT044 VCC–왼쪽 TTL GND. 기존 3.3 V 연결의 기대값은 약 3.3 V다.
- 사용자 결과: “3.2 정도 나오네”. 약 3.2 V의 사용자 보고로 기록한다. 계기 모델·정확도·표시 사진은 미확인이고 전체 전원 품질이나 통신 정상 판정은 아니다.
- 다음 확인은 TXD와 RXD 각각의 TTL GND 기준 DC 전압이다. 이번 턴에 PC 송신·다운로드는 하지 않았다.

### 2026-09-14 — 전원 인가 후 전압 확인부터 진행 요청

- 사용자가 “전원 인가한 후부터 디버깅 … 원해”라고 정정했다. 전원이 켜진 상태의 전압 확인부터 진행하려는 요청으로 반영하며, 이전 저항을 전원 인가 상태에서 측정했다는 확정 보고로 해석하지 않는다.
- 프로브를 회로에서 뗀 뒤 DC 전압 모드, 검정 COM·빨강 V/Ω 단자를 확인하고 AT044의 VCC–TTL GND를 측정하도록 안내한다. 전원 인가 중 저항·도통 모드 사용은 피한다. 전압 실제값은 아직 보고 전이다.

### 2026-09-14 — 저항 표시값 약 10 보고, 단위 미확인

- 사용자 보고: “저항 10정도였어”. 직전 안내 대상은 AT044 VCC–TTL GND이나 실제 측정 지점과 전원 분리 여부는 별도 확인 전이다.
- 단위 없는 보고값을 10 Ω 또는 10 kΩ으로 확정하지 않는다. 전원 인가·추가 송수신은 진행하지 않고 단위와 측정 조건을 확인한다.

### 2026-09-14 — 멀티미터부터 디버깅 재개

- 사용자 요청: “멀티미터 부터 하자”. 보류를 해제하고 전원 분리 후 저항 확인부터 안내한다.
- 대상은 AT044 왼쪽 TTL 헤더의 VCC와 GND. 보드 배선을 유지한 측정은 연결된 전원 회로 전체의 영향을 받으므로 모듈 단독 저항이라고 해석하지 않는다. 아직 측정 결과·통과 판정은 없다.

### 2026-09-14 — 보유 장비 확인 및 디버깅 보류

- 사용자 보고: 멀티미터와 로직애널라이저 보유. 모델·사양은 확인 전이다.
- 사용자 요청: “디버깅 계획만 세우고 이건 나중에 하자.” 위 계획을 작성하고 작업을 보류했다. 이번 요청 이후 추가 실기 시험이나 코드 변경은 수행하지 않았다.

### 2026-09-14 — TX/RX 교환 후 재시험

- 사용자 보고: “tx rx쪽을 뒤집었어”. 펌웨어·핀 기능은 변경하지 않고 기존 이미지로 재시험했다.
- 13:38 COM4 115200 8N1에서 실기 스크립트를 PingCount=3으로 실행했으나 첫 PING이 800 ms 안에 응답하지 않아 중단됐다. 3회 성공으로 기록하지 않는다.
- ST-LINK Hot Plug에서 수신·수신 오류·폐기·송신 오류 카운터 모두 0, USART1 수신 활성 및 PB6 입력/PB7 AF 상태 유지 확인. 보드 연결은 확인됐지만 AT044 단자 전원 실측과 케이블 경로 검증은 아니다.
- [재시험 로그](측정/2026-09-14-RS485-WPF/RS485-실기시험-로그.txt)와 [레지스터 로그](측정/2026-09-14-RS485-WPF/실기-로그.txt)에 추가했다. 다음은 PC 변환기의 종류·A/B 연결을 사진으로 확인하는 것이다.

### 2026-09-14 — 사용자 배선 변경 예정

- 사용자가 “내가 배선 바꿔볼게”라고 알렸다. 변경할 선·최종 연결·완료 여부는 아직 확인하지 않았다.
- 전원을 분리한 상태에서 배선을 변경하도록 안내했다. 변경한 연결을 확인한 뒤 기존 펌웨어로 RS-485 수신과 WPF 표시 시험을 재개한다. 이번 메시지 이후 추가 송수신·다운로드는 하지 않았다.

### 2026-09-14 — RS-485부터 WPF까지 직접 구현 요청

- 사용자: “WPF까지 구현 다 해, UI에 되는 것까지. 단계별 분석은 내가 나중에 할게.” 기존 구현 요청의 범위를 UI까지 확대했다.
- USART1 RX IRQ와 줄 메일박스·수신 만료·오류 폐기·완성 응답 송신, 요청 경로별 파서/응답, WPF RS-485 연결과 기존 진단 화면 연동을 구현했다. RS-485는 기존 ASCII/CRLF의 1:1 요청·응답 방식이며 Modbus 구현을 뜻하지 않는다.
- ARM 빌드·C 호스트 시험·WPF 122개 시험 성공. ST-LINK 최종 다운로드·검증·재시작 성공. COM4 첫 PING부터 응답이 없어서 RS-485 실기 시험은 미완료다. 실제 수신 카운터 0, PB6 입력/PB7 AF 수신 상태를 확인했다.
- 실제 WPF에서 RS-485/COM4 선택과 PING 시간 초과를 확인했고, COM3 UART는 PONG이 표시됐다. TCP PING·W5500·센서 식별·가속도 응답도 확인했다. [구현 기록과 증거](설계/RS485-WPF-구현기록.md).
- PC 쪽 변환기 및 케이블 반대쪽 A/B 연결 확인을 질문했다. 납땜·배선의 사용자 보고와 실기 통신 성공은 계속 구분한다. 공식 관리 문서는 수정하지 않았다.

### 2026-09-14 — USART1 클록 펌웨어 다운로드와 실기 확인

- 사용자 요청으로 STM32CubeProgrammer 2.23.0 / ST-LINK V2J48M35 / SWD 4 MHz를 사용해 연결된 NUCLEO-F401RE에 현재 Debug ELF를 다운로드했다. 빌드는 최신 상태였고, 작업 트리의 기존 spi2.c 변경은 공백 한 칸 추가였다.
- 다운로드 전 Hot Plug에서 RCC_AHB1ENR=0x00000007, RCC_APB2ENR=0x00000000 확인. Under Reset 다운로드·바이트 검증 성공 후 소프트웨어 리셋을 실행했다.
- 재시작 후 Hot Plug 측정: RCC_AHB1ENR=0x00000007(GPIOBEN[1]=1), RCC_APB2ENR=0x00000010(USART1EN[4]=1). 기대한 클록 활성화 상태와 일치한다.
- GPIOB_MODER=0xA9000280에서 MODER6[13:12]=00, MODER7[15:14]=00으로 PB6/PB7 입력 상태를 확인했다. USART1_CR1=0에서 UE[13]/TE[3]/RE[2]=0으로 USART1 송수신 비활성 상태를 확인했다. 통신 성공 판정은 하지 않는다.
- ST-LINK 표시 전압 3.25–3.26 V는 AT044 전원 단자 실측값이 아니다. 새 물리 전원 인가는 수행하지 않았으며 이미 연결된 보드에 다운로드·리셋했다. 기존 저항·TX/RX 방향 미확인 상태는 유지한다.
- [원시 결과 로그와 ELF SHA256](측정/2026-09-14-USART1-클록/다운로드-클록확인-로그.txt). PowerShell의 UTF-8/UTF-16 혼합 저장을 발견해 기존 출력 내용을 유지하며 UTF-8로 통일했다.
- 근거: [RM0368 Rev 6](references/rm0368-stm32f401xb-c-d-e-mb-reference-manual-stmicroelectronics.pdf) §6.3.9 RCC_AHB1ENR p.118–119, §6.3.12 RCC_APB2ENR p.122–123, §8.4.1 GPIOx_MODER p.158, §19.6.4 USART_CR1 p.551–553. 이번 로컬 PDF의 문서 표기와 PDF 페이지는 같다.

### 2026-09-14 — 과제 금지 명시와 USART1 클록 구현

- 사용자가 “너가 구현해줘, 과제 하지 말라고 했잖아”라고 정정하고 규칙 반영 여부를 물었다. 기존 AGENTS.md에는 퀴즈 금지는 있었지만 과제 제시 조항이 남아 있었다. 해당 조항을 과제·숙제·코드 제출 요구 금지와 요청한 범위의 Codex 직접 구현·검증으로 교체했다.
- Src/uart1.c 및 Inc/uart1.h 추가. RCC_AHB1ENR GPIOBEN bit 1과 RCC_APB2ENR USART1EN bit 4를 숫자 시프트와 OR로 설정하고 각 레지스터를 읽어 되돌린다. main의 기존 초기화 순서를 유지하며 app_state_init() 전에 호출하고 cmake/files.cmake에 등록했다.
- 검증: cube-cmake --build --preset Debug 성공. ELF 역어셈블리에서 두 레지스터 읽기→해당 비트 OR→쓰기→읽기를 확인했다. 별도 실기 시험은 하지 않았다. 사용자의 기존 Src/spi2.c 변경을 보존했으며 빌드에는 작업 트리의 해당 변경도 포함됐다.
- 근거: [RM0368 Rev 6](references/rm0368-stm32f401xb-c-d-e-mb-reference-manual-stmicroelectronics.pdf) §6.3.9 RCC AHB1 peripheral clock enable register p.118–119 및 §6.3.12 RCC APB2 peripheral clock enable register p.122–123. PDF 페이지 동일. 핀 설정·송수신 구현은 이번 범위에 포함하지 않았다.

### 2026-09-14 — 측정 미확인 상태로 펌웨어 준비 진행

- 사용자 요청: “다음 넘어가자”. 저항 측정 결과나 점검 완료 보고는 없었다.
- 다음 작은 과제는 RS-485용 USART1 클록 활성화 함수의 사용자 작성으로 안내한다. Codex의 구현 요청으로 해석하지 않았고 펌웨어 파일은 변경하지 않았다.
- VERSION 0.1.0-draft, 요구사항 0.2-draft의 부분 승인 이력과 FW-REQ-005/019/047을 직접 재확인했다. RS-485는 사용자 요청으로 진행 중인 확장 학습이며 공식 범위·판정 변경 없음.
- 제조사 근거: RM0368 Rev 6 §6.3.9 RCC_AHB1ENR p.118–119(GPIOBEN bit 1), §6.3.12 RCC_APB2ENR p.122–123(USART1EN bit 4). 이번에 읽은 로컬 PDF는 문서 표기 페이지와 PDF 페이지가 같다.

### 2026-09-14 — A/B 케이블 직접 납땜 완료 보고

- 직접 납땜 안내 후 사용자가 “다했어 다음은?”이라고 보고했다. AT044 A/B 케이블 직접 납땜 완료의 사용자 보고로 기록한다.
- 패드 상태·절연·기계적 고정·도통 측정·반대쪽 변환기 연결을 각각 검사했다고 확대 해석하지 않는다.
- 다음은 전원 분리 상태의 육안 점검 및 VCC–TTL GND, A+–B- 저항 측정이다. 측정값과 전원 인가 결과는 아직 없다.

### 2026-09-14 — 4심 케이블 연결과 AT044 단자 제거 보고

- 사용자 보고: 케이블 심선은 총 4가닥이며 같은 색 선이 두 가닥씩 있다. 편조체도 있다. 실물 사진은 확인하지 않았다.
- 안내: 서로 꼬인 한 쌍만 A/B에 사용하고, 양 끝에서 같은 도체인지 필요하면 케이블을 기기에서 분리해 도통 측정으로 식별한다. 주황색=A+, 선택한 짝의 흰색=B-는 Codex가 제시한 구분 방법이며 실제 배선 완료를 뜻하지 않는다.
- 남는 두 심선과 편조체 끝은 각각 절연해 두는 임시 방식을 안내했다. 실드·신호 기준 접지의 최종 연결은 USB 변환기 구조 확인 전 미정이다.
- 사용자가 AT044에 달았던 것을 뺐다며 직접 납땜할지 물었다. 대화 문맥상 A/B 단자 제거로 해석했으나 제거 범위·패드 상태는 미검증이다.
- 안내: A/B 패드가 온전하면 직접 납땜할 수 있다. 전원을 모두 분리하고 케이블 끝과 패드 예비 납땜, 짧은 가열, 인접 패드 단락 확인, 케이블 외피의 별도 고정이 필요하다. 직접 납땜 완료·전원 인가·저항 측정 결과는 아직 없다.

### 2026-09-14 — TX/RX 연결 완료 보고

- 직전 추정 배선 표에 대해 사용자가 “오케이 연결함 다음”이라고 보고했다. AT044 TXD↔PB6, RXD↔PB7 연결 완료로 기록한다.
- 이는 물리 연결의 사용자 보고이며 AT044 신호 방향 검증 또는 통신 성공 보고는 아니다.
- 다음 작은 과제로 전원 분리 상태의 AT044 VCC–GND 저항 측정을 안내했다. 측정값은 아직 없으며 전원 인가를 안내하지 않았다.

### 2026-09-14 — 사용자 요청으로 TX/RX 추정안 선택

- 사용자가 Codex에게 추측해서 TX/RX 배선을 정해 달라고 요청했다.
- 임시 선택: STM32 USART1 TX=PB6(CN10-17)→AT044 TXD, AT044 RXD→STM32 USART1 RX=PB7(CN7-21).
- 가정: 모듈의 TXD 표기는 RS-485 송신용 TTL 입력, RXD 표기는 수신한 데이터의 TTL 출력이다. 제공 이미지로 입출력 방향을 확정한 것은 아니다.
- 추정과 다르면 출력끼리 연결될 수 있으므로, 배선은 전원 분리 상태로 진행하며 전원 인가·송수신 구동 전 방향 확인이 남아 있다. 펌웨어 변경·실제 배선 완료·통신 성공은 아직 없다.

### 2026-09-14 — TX/RX를 제외하고 먼저 배선

- 사용자 결정: TX/RX 연결은 제외하고 나머지 배선을 먼저 진행한다.
- 안내 범위: 전원을 분리한 상태에서 NUCLEO CN7-16(3.3V)→AT044 VCC, CN7-20(GND)→AT044 TTL 쪽 GND. PC용 변환기의 A+/B- 표기가 일치하면 AT044 A+↔변환기 A+, B-↔B-로 연결한다.
- AT044 버스 쪽 접지 기호 단자와 TXD/RXD는 이번 연결에서 비워 둔다. USB 변환기의 5V를 AT044 VCC에 추가 연결하지 않는다. 전원 인가는 배선 점검 후 별도 진행한다.
- 배선 완료·전원 인가·측정·통신 성공 보고는 아직 없다.

### 2026-09-14 — RS-485 배선 안내

- 사용자 요청: RS-485 배선을 직접 안내해 달라고 요청했다.
- Codex 제안: 기존 USART2 진단(PA2/PA3)과 초음파(PA10)를 유지하기 위해 USART1의 PB6(TX)/PB7(RX)를 사용한다. 사용자 선택·배선 완료로 판정하지 않는다.
- NUCLEO 위치: PB6=CN10-17, PB7=CN7-21, 3.3V=CN7-16, GND=CN7-20. 근거: [UM1724 Rev 17](references/dm00105823-nucleo-f401re-user-manual-stmicroelectronics.pdf), Hardware layout and configuration, Table 29 p.59; [DS10086 Rev 5](references/DS_stm32f401re.pdf), Pinouts and pin description, Table 8 p.43 및 Table 9 p.46. 모두 문서 표기와 PDF 페이지가 같다.
- [AT044 공급사 자료](https://www.devicemart.co.kr/goods/view?no=1361352)의 상세 이미지에서 VCC/TXD/RXD/GND, A+/B-/접지 단자를 확인했다. 3.3V 지원·자동 방향 제어·R0 단락 시 120Ω 종단 활성화 설명을 확인했다. 공급사 이미지에 TXD/RXD 입출력 방향을 명시한 회로도는 없어 실제 모듈의 방향 대조는 남아 있다.
- 전원은 NUCLEO 3.3V→VCC, GND→TTL 쪽 GND로 제안한다. 신호선은 AT044 TXD가 입력, RXD가 출력인 경우 PB6→TXD, RXD→PB7로 연결한다. PC 쪽은 A+↔A+, B-↔B- 기준이며 실제 USB 변환기 모델·단자 확인은 남아 있다.
- 현재 USART1 펌웨어 구현·전원 인가·배선 완료·통신 성공은 기록하지 않는다. 이번 안내는 전원을 분리한 상태의 배선 준비다.

### 2026-09-14 — RS-485 재개 위치 정정

- 사용자가 “RS-485 배선하기로 했고 납땜도 다 했다”고 정정했다. 다음 진행은 RS-485 배선으로 정해져 있으며, 납땜은 사용자 보고 기준 완료다. 실제 납땜 작업일은 별도로 확인하지 않았다.
- Codex가 9/11 학습 계획만 보고 후속 학습 순서가 미정이라고 안내한 것은 현재 진행 위치를 반영하지 못한 설명이었다.
- 사용자가 이런 진행 내용을 계속 남기는 Markdown 기록을 요청하여 이 파일을 만들었다.
- 배선 완료 여부, 납땜 품질 검사, 전원 인가 및 RS-485 송수신 시험 결과는 이번 보고에 포함되지 않았다.
- 이전 코드 분석·실기 이력과 장기 학습 계획: [통신 경험 요약](통신-경험과-채용공고-대응.md).
