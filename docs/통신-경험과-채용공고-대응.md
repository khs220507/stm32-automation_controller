# 채용 공고 대응 요약

경험 요약은 2026-09-14 갱신. Codex를 도구로 활용한 프로젝트 경험을 포함하며, 전체 실기 검증 완료를 뜻하지 않는다. 최신 과제와 다음 행동은 [작업 이어가기](작업-이어가기.md)를 먼저 확인한다.

- 최신 상태: Codex가 구현한 RS-485 USART1 및 WPF 기능에 대해, 2026-09-14 재배선 후 COM6 실기 스크립트에서 PING/PONG 3회, 상태·장치 식별 응답 및 가속도 원시값 수신을 확인했다. 초음파는 TIMEOUT이며 RS-485 센서의 WPF 화면 표시는 아직 미확인이다. 이전 COM4 무응답 이후 첫 RS-485 통신 성공이며 원인을 배선 하나로 확정하지 않는다. [실기 로그](측정/2026-09-14-RS485-WPF/RS485-실기시험-로그.txt), [현재 위치](작업-이어가기.md). 단계별 사용자 코드 분석은 나중에 진행하기로 했다.

- 2026-09-14 사용자 보고: RS-485 작업을 위한 납땜을 완료했으며, 이어서 RS-485 배선을 진행하기로 했다. 배선·통신 시험 완료 판정은 아니다.
- 2026-09-14 후속 사용자 보고: AT044 TXD↔PB6, RXD↔PB7 연결 완료. 모듈의 입출력 방향은 추정 상태이며 전원 인가 전 점검·통신 시험은 아직이다. 세부 재개 위치는 [작업 이어가기](작업-이어가기.md)를 따른다.
- 2026-09-14 후속 사용자 보고: AT044 단자 제거 후 RS-485 A/B 케이블 직접 납땜 완료. 납땜 품질·단락 측정·통신 성공은 아직 검증하지 않았다.
- 2026-09-14 사용자 요청으로 Codex가 RS-485용 USART1 클록 활성화 함수와 부팅 호출을 구현했다. Debug 빌드·생성 코드 확인 성공. 핀·통신 설정과 실기 검증은 아직이다.
- 2026-09-14 후속: ST-LINK로 다운로드·검증·재시작 성공. RCC_APB2ENR의 USART1 클록 활성화와 PB6/PB7 입력·USART1 송수신 비활성 상태를 실제 레지스터로 확인했다. RS-485 통신 시험은 아직이다. [실측 로그](측정/2026-09-14-USART1-클록/다운로드-클록확인-로그.txt).

- 2026-09-14 Codex 구현·검증: WPF에서 TCP·RS-485·UART 개별 시험과 센서 화면을 분리했다. 128개 자동 시험과 실제 각 버튼의 PONG, 타 경로 결과 유지·개별 로그를 확인했다. 동일 COM 충돌·시간 초과·에코·분할 응답도 검증했다. 사용자 코드 분석 완료를 뜻하지 않는다. [실기 증거](측정/2026-09-14-RS485-WPF/통신-개별시험-실기로그.txt).

## 해본 것

| 공고 관련 기술 | 경험 |
|---|---|
| C·베어메탈 | 레지스터 제어, 비트 연산, 포인터·구조체 |
| STM32·ARM | GPIO, UART, I2C, 타이머 |
| UART | 기존 송수신 구현·분석, 독립 PING/PONG 진단 복구 및 TCP 센서 통신과 동시 실기 확인 |
| I2C | 센서 설정·가속도 읽기 |
| SPI | SPI2 설정·1바이트 전송, W5500 버전 실제 읽기·WPF 표시 확인 |
| 드라이버 개발 | UART·I2C·센서 드라이버 구현·분석, 오류 처리 |
| Ethernet/TCP | W5500 소켓 서버·WPF TCP 클라이언트, 명령·센서값 실제 LAN 송수신 |
| API 활용 | CMSIS·WPF 시리얼 통신 및 TcpClient/NetworkStream 비동기 API |
| 타이머 | 시간 측정·초음파 입력 캡처 코드 |
| 데이터 처리 | XYZ 변환·g 환산·거리 계산 |
| 상태·오류 처리 | 상태 머신 기초·시간 초과·오류 반환 |
| 디버깅 | ST-LINK·중단점·레지스터 관찰 |
| 로직 분석기 | 직접 I2C 파형 캡처·해독 |
| 데이터시트 | 핀·클록·레지스터 대조 |
| PC 연동 | WPF 가속도 표시·정지·재연결 |
| 개발·기록 | CubeIDE·VS Code·Git·시험 기록 |

## 학습 재개 기준 — 2026-09-11

아래는 9/11 당시의 분석 완료 범위와 학습 계획이다. 현재 재개 위치는 [작업 이어가기](작업-이어가기.md)를 우선한다.

- AI 학습 대상 확정(사용자 결정): **현재 NUCLEO-F401RE에서 작은 AI 모델을 직접 실행하는 온디바이스 추론을 학습한다.** PC에서 학습한 모델을 보드에 넣는 방향이며, AI용 새 보드 구매는 현재 선택하지 않았다. MPU6050을 이용한 동작·진동 분류는 제안한 예시로, 구체적인 분류 목표·모델·도구·진행 순서는 미정이다. 모델 학습·변환·펌웨어 적용·메모리 및 처리 시간 검증은 아직 수행하지 않았다.
- 통신 학습 후속 요청: 사용자가 **Modbus·CAN·MQTT 모두 학습할 의사**를 밝혔다. 기존 통신 범위 유지 방침에 대한 사용자 요청으로 추가하며, 세부 구성·구매·구현은 미확정이다. 아래의 Modbus 미결정 표기는 이전 부품 검토 당시 상태다. FreeRTOS 학습 계획도 유지한다.
- 최신 학습 계획(사용자 요청): **FreeRTOS 학습을 추가한다.** 별도 실습에서 태스크·실행 주기를 익히고, 이후 큐·뮤텍스와 기존 센서·통신 처리 연결을 학습하는 방향이다. 개념 설명만 진행했으며 환경 구성·구현·실기 검증은 아직이다. 기존 펌웨어의 RTOS 전환이나 공식 요구사항 변경을 뜻하지 않는다.
- 통신 학습 범위는 더 늘리지 않는다. 새 통신 방식·프로토콜은 사용자 요청이나 구체적인 필요가 생길 때 검토한다. 기존 RS-485 및 ESP32 Wi-Fi/BLE 계획은 유지하되, 완료한 W5500·TCP·프로토콜 분석을 반복하지 않는다. FreeRTOS 추가로 기존 통신 계획을 취소한 것은 아니다.
- 사용자 확인: W5500과 TCP(`Src/tcp_link.c`) 분석은 이미 마쳤다. 이전 기록의 분석 대기 안내를 현재 미완료 항목으로 사용하지 않는다. 세부 함수별 분석 일시는 이번 대화에서 확인하지 않았다.
- 이번 대화에서 `protocol.c`의 주요 부분을 설명했다: `protocol_poll_command()`의 바이트 수신·버퍼 유지·CRLF 완료·잘못된 줄 폐기, `protocol_parse_command()`의 `strcmp(...) == 0`과 명령 반환, 수신 초기화 후 지역 변수 반환, `protocol_send_accel()`의 응답 구성과 센서 읽기의 구분.
- `protocol_send_i16()`와 `app_state.c`의 명령 실행 연결을 남은 범위로 안내한 뒤 사용자가 “다 봤다”고 확인했다. 이 두 부분은 사용자 분석 완료 보고로 기록하며, Codex가 이번 대화에서 상세 설명한 항목과 구분한다. `protocol.c` 주요 분석은 마무리했다.
- 기존 통신 계획(2026-09-11): STM32는 **PC와 RS-485 통신**하는 방향으로 부품을 검토했다. AT044는 구매 완료 기록이 있으며 PC용 USB-RS485 변환기는 구매 후보를 안내한 상태로 구매·수령 확인 전이다. 배선·구현·실기 시험 전이며 Modbus RTU 도입은 미결정이다. 완료한 W5500·TCP·프로토콜 분석을 다시 제안하지 않는다.
- 무선 학습은 배선 중복을 피하려는 사용자 의사에 따라 **ESP32 별도 프로젝트**로 분리한다. ESP32↔STM32 RS-485 및 추가 ESP32를 이용한 무선 연결은 비교한 후보이며 확정한 구성이 아니다. PC↔ESP32 BLE/Wi-Fi를 별도 학습하고, STM32와의 연동은 이후 선택 사항으로 둔다.
- 사용자가 PC에 Bluetooth를 설치했다고 보고했다. 앞서 조회한 어댑터는 오류 코드 43이었으나 설치 이후 상태는 재확인 전이다. BLE 검색·연결·데이터 송수신 성공으로 기록하지 않는다.
- 분석 완료와 실기 검증은 별개다. 이번 기록은 새 시험 통과나 공식 요구사항 판정을 뜻하지 않는다. 사용자 요청 없이 퀴즈·이해도 확인을 하지 않는다.

## 이전 구현·시험 이력

아래의 ‘최신’, ‘현재’, ‘다음 분석’은 각 기록 당시의 표현이다. 학습 재개에는 위의 최신 기준을 우선한다.

- 최신 상태(2026-09-10 후속): 사용자 요청으로 Codex가 UART 독립 진단을 복구했다. WPF에서 COM3 → UART 확인으로 PING/PONG을 시험하며 LAN TCP 센서 통신과 동시 사용 가능하다. RXNE/TXE 인터럽트, 길이·수신 오류·부분 입력 만료 처리와 독립 UI/로그를 구현했다. UART 단독/동시 TCP 가속도 100회/TCP 종료 후 UART 및 실제 WPF 표시 확인. 호스트 시험·WPF 119개 통과. [실측 로그](측정/2026-09-10-UART-TCP/UART-TCP-실측-로그.txt) · [화면](측정/2026-09-10-UART-TCP/UART-TCP-WPF-실측화면.png). 사용자 코드 분석·파형·장시간 검증은 별도다. 아래 TCP 전환 기록의 UART 미사용 상태는 이번 복구로 대체됐다.

- 최신 상태(2026-09-10): 사용자 요청으로 Codex가 UART PC 연결을 LAN TCP로 전환했다. W5500 socket 0 서버 `169.254.100.2:5000`과 WPF 비동기 TCP 클라이언트, CRLF 분할·다중 수신, 연결별 초기화, 유한 대기·재접속을 구현했다. W5500 하드웨어 스택을 제어했으며 TCP/IP 스택을 직접 작성한 것은 아니다. 구현과 실기 확인은 수행했으나 사용자 코드 분석·이해 완료 판정은 하지 않았다.
- STM32·WPF 빌드 및 호스트 시험 성공, WPF 119개 통과. 실제 LAN에서 PING 300회·재접속 10회·분할/동시 명령과 WPF의 PONG/버전/가속도 표시를 확인했다. 초음파는 TIMEOUT으로 거리 측정 성공 아님. [실측 로그](측정/2026-09-10-TCP/TCP-LAN-실측-로그.txt) · [화면](측정/2026-09-10-TCP/TCP-WPF-실측화면.png) · [설계·한계·실패 수정 기록](설계/I2C-MPU6050-진행기록.md). 다음 분석 위치는 `Src/tcp_link.c` 초기화·소켓 상태 처리다. 아래 항목은 이전 작업 시점의 이력이다.

- 2026-09-10 W5500 WPF 연결 확인 명령과 별도 로그를 구현하고 실제 COM3에서 `OK,CHECK_W5500,4`를 수신했다. Mica 화면 변경 후 재연결에서도 실제 0x04 표시를 확인했다. [실제 TX/RX 로그](측정/2026-09-10-W5500/W5500-실측-로그.txt) · [실측 화면](측정/2026-09-10-W5500/W5500-마이카-실측화면.png). SPI 파형·실기 고장·TCP/IP·장시간 검증은 아직이다.
- 사용자 요청으로 전체 스크롤 없는 컴팩트 WPF 화면과 내장 Fluent/Mica를 적용했다. 펌웨어는 부팅 자동 진단에서 버튼 요청 방식으로 전환했고 원시 버전과 일치 여부를 구분한다. STM32·WPF 빌드, 호스트 시험, WPF 118개 시험 성공. 기록과 시험 한계는 [진행 기록](설계/I2C-MPU6050-진행기록.md)의 최신 항목에 남겼다.
- 다음 대화: `Src/w5500.c`의 `w5500_read_version()`에서 주소·제어·더미 바이트 구성을 분석한다. `spi2_transfer_byte()` 본문은 설명했으며 사용자는 대기 함수 내부의 추가 분석을 생략했다. 별도 이해 완료 판정 없음.
- 2026-09-10 사용자 요청으로 Codex가 W5500 VERSIONR 읽기와 부팅 1회 호출을 구현했다. RDY 대기, 프레임 전체 제한시간, CS 해제, 실패 시 출력값 보존·재호출 차단을 포함한다. 디버거에서 전송 결과·원시값·기대값 일치를 구분한다. ARM Debug 빌드와 W5500·기존 호스트 시험 성공. 다운로드·실제 응답·파형 검증은 아직이다. 상세 근거와 부팅 지연 한계는 [진행 기록](설계/I2C-MPU6050-진행기록.md)의 2026-09-10 항목에 남겼다.
- 사용자 요청으로 Codex가 1바이트 송수신과 공통 시간 제한·오류 반환·실패 시 출력값 보존을 구현했다. 오류 후 재부팅 전 재전송 차단. ARM Debug 빌드 및 SPI2·기존 I2C·MPU6050 호스트 시험 성공. CS 프레임 제어·W5500 응답 판독·실기 송수신은 아직이다. 상세 근거와 시험 한계는 [진행 기록](설계/I2C-MPU6050-진행기록.md)의 2026-09-09 SPI 1바이트 송수신 구현 항목에 기록했다.
- 현재 SPI 단계: [spi2_configure()](../firmware/automation_controller_vscode/automation_controller/Src/spi2.c) 구현. Master·8비트·MSB 우선·Mode 0·PCLK1/16·GPIO CS 제어, SPI2 활성화까지 완료. 사용자 요청 범위는 통신 설정까지이며 송수신 시험 전.
- 설정 검증: Debug 빌드·다운로드 검증·리셋 후 STM32CubeProgrammer 2.23.0/ST-LINK SWD Hot Plug에서 SPI2 CR1=0x035C, CR2=0, SR=0x0002, I2SCFGR=0, RCC CFGR=0 확인. 의도한 설정과 일치하고 PB12(CS) HIGH·PB13(SCLK) LOW도 GPIO 입력값에서 확인. 현재 HSI 명목 16MHz·AHB/APB1 분주 1이므로 SCLK 계산값은 16MHz/16=1MHz. 실측 주파수가 아니며 HSI 오차 영향을 받음. 근거: [RM0368 Rev 6](references/rm0368-stm32f401xb-c-d-e-mb-reference-manual-stmicroelectronics.pdf) §20.5.1~20.5.3 p.602~606, §20.5.8 p.608. 증거는 이번 대화의 도구 출력.

- SPI: WIZ550io 전원·GND, MOSI/PB15·MISO/PB14·SCLK/PB13·SCSn/PB12, RSTn/PC4·RDY/PB1 배선 완료(사용자 확인). 전원 인가 전 점검 중 전원선을 GND에 잘못 연결한 것을 발견·수정(사용자 확인). 전원 측정 후 사용자 보고값 3.2V. SPI 통신 시험 전.
- 전원 확인: 멀티미터(모델 미확인), ST-LINK USB 공급·초음파/가속도/WIZ550io 연결 조건에서 WIZ550io J1-7(3V3D)–J1-1(GND) DC 전압 측정을 안내했고 사용자가 3.2V를 보고했다. 기대 약 3.3V, 제조사 허용 2.97~3.63V. [제조사 Electrical Characteristics](https://docs.wiznet.io/Product/ioModule/wiz550io). 증거는 대화의 사용자 보고이며 사진·별도 계측 파일은 없음.

- RDY 확인: 같은 멀티미터 DC 전압 측정 안내에 따라 J2-2(RDY)–GND에서 사용자 보고값 3.2V. [제조사 J2 — Control & Status](https://docs.wiznet.io/Product/ioModule/wiz550io)의 초기화 완료 HIGH 신호와 부합. 증거는 대화의 사용자 보고이며 별도 계측 파일 없음.
- SPI2 클록: [spi2_enable_clocks()](../firmware/automation_controller_vscode/automation_controller/Src/spi2.c)를 추가하고 main에서 호출. Debug 빌드·ST-LINK 다운로드/검증·리셋 완료. STM32CubeProgrammer 2.23.0의 SWD Hot Plug 읽기에서 RCC_AHB1ENR=0x00000007, RCC_APB1ENR=0x00024008 관찰(기대: GPIOB bit 1·GPIOC bit 2·SPI2 bit 14가 각각 1). 근거: [RM0368 Rev 6](references/rm0368-stm32f401xb-c-d-e-mb-reference-manual-stmicroelectronics.pdf) §6.3.9 p.118~119, §6.3.11 p.119~120. 증거는 이번 대화의 도구 출력. 핀 모드 설정·SPI 송수신은 아직 미구현.

- SPI2 핀 설정: [spi2_pins_init()](../firmware/automation_controller_vscode/automation_controller/Src/spi2.c) 추가, Debug 빌드·다운로드·검증·리셋 완료. CS/PB12·RSTn/PC4는 HIGH 일반 출력, RDY/PB1은 입력, PB13~15는 AF5로 설정. 아직 SPI 송수신 전.
- 핀 설정 확인: STM32CubeProgrammer 2.23.0·ST-LINK SWD Hot Plug에서 GPIOB MODER=0xA9000280, AFRH=0x55500000, IDR=0x0000F79F, ODR=0x00001000; GPIOC MODER=0x00000100, IDR=0x0000FFFF, ODR=0x00000010; SPI2 CR1=0 관찰. 대상 모드·AF5·CS/RSTn/RDY HIGH와 일치하며 SPI2 비활성 상태. 근거: [RM0368 Rev 6](references/rm0368-stm32f401xb-c-d-e-mb-reference-manual-stmicroelectronics.pdf) §8.4.1~8.4.7 p.158~161, §8.4.10 p.163 및 [DS10086 Rev 5](references/DS_stm32f401re.pdf) Table 9 p.46. 증거는 이번 대화의 도구 출력이며 파형 시험은 미수행.

## 아직 안 했거나 미완료

- SPI 파형 검증·TCP 실기 고장/장시간 검증
- 센서 인터럽트·DMA·RTOS
- 각속도·센서 보정·기울기/진동 경고
- 모터 제어·제어 알고리즘 튜닝
- 실기 고장·복구·장시간 시험
- 전체 자동운전 통합
- WIZnet 통신 라이브러리 연동
- IAR·Keil·AVR·TMS320

근거: [목표 공고](00-목표-채용공고.md) · [개발 기록](records/펌웨어-프로젝트-요구사항.xlsx) · [진행 기록](설계/I2C-MPU6050-진행기록.md)
